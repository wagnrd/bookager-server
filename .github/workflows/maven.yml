name: Bookager Server

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Set up JDK 11
      uses: actions/setup-java@v1
      with:
        java-version: 11
    - name: Build with Maven
      run: mvn -B package --file pom.xml deploy -Dregistry=https://maven.pkg.github.com/wagnrd -Dtoken=61f02f7e360dab0802316c5893d4e87f7ad274e3
